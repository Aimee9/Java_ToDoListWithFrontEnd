<h1>$name.getName()</h1>

<p>Here is your list of Tasks:</p>
<ol>
#foreach ($taskName in $task)
  <ol><a href="category/$name.getId()/task/$name.getId()">$taskName.getName()</a></ol>
#end
</ol>

<p>Add a new Task:</p>
<form action="/category/$name.getId()/task" method="post">
 <label for="taskName">Task:</label>
  <input id="taskName" name="taskName" type="text">
  <button type="submit" class="btn btn-default">Add Task</button>
</form>
